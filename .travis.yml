
language: cpp

sudo: required

dist: trusty

before_install:
  - sudo apt-get install libboost-all-dev

install:
  # empty -- all headers

script:
  - cd check
  - g++ -I.. main.cpp checkduplicatesymbols1.cpp checkduplicatesymbols2.cpp -o main
  - cd ../test-suite
  - g++ -I.. -o quantlibtestsuite quantlibtestsuite.cpp
  - ./quantlibtestsuite --log_level=message

notifications:
  email:
    on_success: change
    on_failure: change
  
